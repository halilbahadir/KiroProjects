# ============================================================================
# Shopping Assistant Chatbot Service - Environment Configuration
# ============================================================================
# This file contains all environment variables required to run the service.
# Copy this file to .env and fill in your actual values.
#
# SECURITY WARNING: Never commit the .env file to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# AWS Credentials (REQUIRED)
# ----------------------------------------------------------------------------
# These credentials are used to authenticate with Amazon Bedrock for AI model access.
# You can obtain these from the AWS IAM console or your AWS administrator.
#
# For temporary credentials (e.g., from AWS SSO), include AWS_SESSION_TOKEN.
# For permanent IAM user credentials, AWS_SESSION_TOKEN is not needed.

# Your AWS Access Key ID (20 characters, starts with AKIA)
# COMMENTED OUT - Using AWS CLI default credentials instead
# To use explicit credentials, uncomment and fill in the values below
# AWS_ACCESS_KEY_ID=

# Your AWS Secret Access Key (40 characters)
# AWS_SECRET_ACCESS_KEY=

# AWS Session Token (optional, only for temporary credentials from AWS SSO/STS)
# Leave empty or remove this line if using permanent IAM credentials
# AWS_SESSION_TOKEN=

# AWS Region where Bedrock service is available
# Supported regions: us-east-1, us-west-2, eu-west-1, ap-southeast-1, etc.
# Default: us-west-2
AWS_REGION=us-west-2

# ----------------------------------------------------------------------------
# Amazon Bedrock Configuration
# ----------------------------------------------------------------------------
# Configuration for the Bedrock Nova Pro language model.

# Bedrock Model ID - specifies which AI model to use
# Available models:
#   - us.amazon.nova-pro-v1:0 (recommended, balanced performance)
#   - us.amazon.nova-lite-v1:0 (faster, lower cost)
#   - us.amazon.nova-micro-v1:0 (fastest, lowest cost)
# Default: us.amazon.nova-pro-v1:0
BEDROCK_MODEL_ID=us.amazon.nova-pro-v1:0

# Model Temperature - controls response randomness (0.0 to 1.0)
# Lower values (0.0-0.3): More focused and deterministic responses
# Medium values (0.4-0.7): Balanced creativity and consistency (recommended)
# Higher values (0.8-1.0): More creative and varied responses
# Default: 0.7
BEDROCK_TEMPERATURE=0.7

# Maximum Tokens - limits the length of model responses
# Typical values: 512 (short), 1024 (medium), 2048 (long), 4096 (very long)
# Note: Higher values increase latency and cost
# Default: 2048
BEDROCK_MAX_TOKENS=2048

# ----------------------------------------------------------------------------
# HTTP Server Configuration
# ----------------------------------------------------------------------------
# Configuration for the FastAPI web server.

# Server Host - IP address to bind the server to
# Use 0.0.0.0 to accept connections from any network interface
# Use 127.0.0.1 to accept only local connections
# Default: 0.0.0.0
SERVER_HOST=0.0.0.0

# Server Port - TCP port for the HTTP server
# Common ports: 8000 (development), 80 (HTTP), 443 (HTTPS)
# Must be between 1 and 65535
# Default: 8000
SERVER_PORT=8000

# ----------------------------------------------------------------------------
# Backend E-commerce API Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Configuration for connecting to the existing e-commerce backend.

# Backend API Base URL - full URL to the e-commerce API
# Examples:
#   - Local development: http://localhost:3001
#   - Docker: http://host.docker.internal:3001
#   - Remote: https://api.example.com
# Must start with http:// or https://
BACKEND_API_URL=http://localhost:3001

# Backend API Key (optional) - authentication key for backend API
# Leave empty if the backend doesn't require authentication
# If your backend requires an API key, set it here
BACKEND_API_KEY=

# ----------------------------------------------------------------------------
# Logging Configuration
# ----------------------------------------------------------------------------
# Controls the verbosity of application logs.

# Log Level - minimum severity level for log messages
# Available levels (from most to least verbose):
#   - DEBUG: Detailed information for diagnosing problems
#   - INFO: General informational messages (recommended for production)
#   - WARNING: Warning messages for potentially harmful situations
#   - ERROR: Error messages for serious problems
#   - CRITICAL: Critical messages for very serious errors
# Default: INFO
LOG_LEVEL=INFO

# ============================================================================
# Additional Notes
# ============================================================================
#
# Docker Usage:
# - When running in Docker, use host.docker.internal instead of localhost
#   for BACKEND_API_URL if the backend runs on your host machine
# - Example: BACKEND_API_URL=http://host.docker.internal:3001
#
# AWS Permissions Required:
# - bedrock:InvokeModel
# - bedrock:InvokeModelWithResponseStream
#
# Testing:
# - For local testing, you can use AWS credentials from ~/.aws/credentials
# - For CI/CD, use IAM roles or temporary credentials
#
# Security Best Practices:
# - Never commit .env files to version control
# - Rotate AWS credentials regularly
# - Use IAM roles when deploying to AWS infrastructure
# - Restrict AWS credentials to minimum required permissions
# - Monitor CloudWatch logs for suspicious activity
#
# ============================================================================
